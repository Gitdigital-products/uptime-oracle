Each repo will get a custom .env.example, but here’s a universal starter:
 
# Database
DATABASE_URL=
 
# Authentication
JWT_SECRET=
 
# External Services
API_KEY=
WEBHOOK_URL=
 
# Email
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
 
# Misc
NODE_ENV=development
PORT=3000